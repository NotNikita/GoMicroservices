syntax = "proto3";
package logs;
option go_package = "/logs";

service LogService {
    rpc LogInfo (LogRequest) returns (LogResponse);
}

message LogRequest {
    string name = 1;
    string data = 2;
}

message LogResponse {
    string result = 1;
}